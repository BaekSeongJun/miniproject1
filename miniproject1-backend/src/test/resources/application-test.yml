spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pharmaprice_test
    username: ${POSTGRES_USER:pharmaprice}
    password: ${POSTGRES_PASSWORD:qwe123}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul
  flyway:
    enabled: true
    locations: classpath:db/migration

jwt:
  secret: test-jwt-secret-key-minimum-32-bytes-long-for-hs256
  access-token-validity-seconds: 1800
  refresh-token-validity-seconds: 1209600
